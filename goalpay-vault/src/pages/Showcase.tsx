import React from 'react';
import { AcornsShowcase } from '@/components/AcornsShowcase';

/**
 * Showcase Page
 * 
 * Landing page that showcases both the existing goal-based savings
 * and the new Acorns micro-investing features.
 */
const Showcase: React.FC = () => {
  return <AcornsShowcase />;
};

export default Showcase;
